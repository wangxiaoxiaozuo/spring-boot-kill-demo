server:
  port: 9003
  servlet:
    context-path: '/cart'

web:
  swagger:
    base-package-path: com.kill.core.controller
    tittle: '秒杀-购物车'

spring:
  profiles:
    active: local
  application:
    name: kill-shopping-cart
  cloud:
      nacos:
          discovery:
              server-addr: localhost:8848

